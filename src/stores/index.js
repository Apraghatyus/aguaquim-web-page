/**
 * stores/index.js - Barrel export de todos los stores Pinia.
 *
 * Centraliza las exportaciones para importar desde un solo lugar:
 *   import { useAuthStore } from '@/stores'
 */

export { useAuthStore } from './auth.js'
